# generic grade calculator to make grade calculations easier

def main():
    # determine number of grade categories
    numCategories = input('Enter number of grading categories: ')

    # determine each categories grade percentage
    percentString = input('Enter category names (NO SPACES WITHIN CATEGORIES): ')

    

if __name__ == '__main__':
    main()
